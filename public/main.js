google.visualization.components.register({
    id: "network_graph_ipdp",
    init: (dom, config) => {
        dom.innerHTML = "<h2>Network Graph IPDP</h2><p>Plugin cargado exitosamente.</p>";
    }
});
